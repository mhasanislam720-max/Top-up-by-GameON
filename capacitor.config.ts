import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId:'bd.gametopup.app', appName:'Game Top-Up BD', webDir:'dist', bundledWebRuntime:false };
export default config;
